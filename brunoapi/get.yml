info:
  name: get
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/tasks?status=incompleted
  headers:
    - name: "5"
      value: afficher tache
  params:
    - name: status
      value: incompleted
      type: query
  body:
    type: json
    data: '{"id": 5,  "title": "nouvelle tache","completed": false}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
